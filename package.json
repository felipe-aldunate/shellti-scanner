{
  "name": "shellti-scanner",
  "version": "1.0.0",
  "description": "Auditoría de privacidad y performance web - ShellTI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npx playwright install chromium --with-deps"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "groq-sdk": "^1.2.1",
    "openai": "^6.42.0",
    "playwright": "^1.60.0"
  }
}
